var spacer = $('.card.spacer');

$('.card.spacer').remove();
$('#providers').append("<%= j(render( @providers )) %>");
$('#providers').append(spacer);

$('.pagination').replaceWith("<%= j paginate(@providers) %>");
$('.pagination').css("display", "none");	// if we're using js (so end up here) we don't want to see the paginator
